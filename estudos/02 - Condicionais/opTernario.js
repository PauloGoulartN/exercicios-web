let num = 0

let resposta = num % 2 === 0
    ? num === 0
        ? "é zero"
        : num + " é Par"
    : num + " é Impar"

console.log(resposta)

let idade = 22

let permissao = idade >= 18
    ? "Acesso Liberado"
    : "Acesso Negado"

console.log(permissao)